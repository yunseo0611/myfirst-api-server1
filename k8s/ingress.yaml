apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  annotations:
    cert-manager.io/cluster-issuer: letsencrypt-prod
  name: sk117-myfirst-ingress
  namespace: skala-practice
spec:
  ingressClassName: public-nginx
  rules:
  - host: sk117-ingress.skala25a.project.skala-ai.com
    http:
      paths:
      - backend:
          service:
            name: sk117-custom-jenkins
            port:
              number: 8080
        path: /jenkins
        pathType: Prefix
      - backend:
          service:
            name: sk117-myfirst-api-server
            port:
              number: 8080
        path: /
        pathType: Prefix
  tls:
  - hosts:
    - 'sk117-ingress.skala25a.project.skala-ai.com'
    secretName: sk117-ingress-project-tls-cert
